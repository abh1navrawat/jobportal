spring:
  application:
    name: auth-service
  datasource:
    url: jdbc:mysql://localhost:3306/job_find_auth?createDatabaseIfNotExist=true
    username: root
    password: 1crazy!
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

server:
  port: 7778

eureka:
  client:
    service-url:
      defaultZone: http://localhost:7800/eureka/
